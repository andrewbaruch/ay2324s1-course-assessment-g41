apiVersion: v1
kind: Service
metadata:
  name: user-service
  labels:
    app: user
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 3000
  selector:
    app: user
  type: ClusterIP
